server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # HashRouter (App.tsx uses #/pdfs etc.) — all paths serve index.html,
    # routing happens client-side via the URL fragment.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
